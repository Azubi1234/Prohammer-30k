name: Diagnose special size parents
on:
  push:
    paths:
      - '.github/build/r56_diag_special_size_parents.py'
  workflow_dispatch:
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/build/r56_diag_special_size_parents.py
