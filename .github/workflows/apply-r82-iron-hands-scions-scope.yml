name: Apply Revision 82 Iron Hands Scions scope
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/build/rev82_iron_hands_scions_scope.py'
      - '.github/workflows/apply-r82-iron-hands-scions-scope.yml'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions: {contents: write}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.x'}
      - run: python .github/build/rev82_iron_hands_scions_scope.py
      - run: python .github/build/r56_audit_all_unit_sizes.py
      - name: Verify
        run: |
          python - <<'PY'
          import xml.etree.ElementTree as ET
          c=ET.parse('Legiones Astartes.cat').getroot();g=ET.parse('Prohammer 30k.gst').getroot()
          assert c.get('revision')=='82' and c.get('gameSystemRevision')=='49' and g.get('revision')=='49'
          print(open('inspection-r82-iron-hands-scions-scope.txt',encoding='utf-8').read())
          PY
      - name: Commit
        run: |
          git config user.name 'Azubi1234'
          git config user.email '132048044+Azubi1234@users.noreply.github.com'
          git add 'Legiones Astartes.cat' 'Prohammer 30k.gst' index.xml inspection-r82-iron-hands-scions-scope.txt inspection-r56-all-unit-sizes.txt
          git diff --cached --quiet || git commit -m 'Revision 82: scope Iron Hands Scions transports correctly'
          git pull --rebase origin main
          for i in 1 2 3 4; do git push && break || { sleep 2; git pull --rebase origin main; }; done
