name: Refresh New Recruit
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh New Recruit
        run: curl --fail --show-error --request GET "https://www.newrecruit.eu/api/check-for-update/Azubi1234/Prohammer-30k"
